# Changelog

这个文件记录项目的主要发布变更。

## [Unreleased]

## [0.4.0] - 2026-05-15

### Added

- 新增设置页，支持主题模式、默认保存目录、默认代理、默认下载格式、文件名模板、重试次数、并发片段等持久化设置。
- 新增下载格式预设，支持最佳画质、最高 1080p、最高 720p、最高 480p 和仅音频 MP3。
- 新增文件名模板选项，支持标题、标题加 ID、作者加标题、日期加标题。
- 新增下载取消能力。
- 新增更完整的下载状态展示，包括队列进度、当前文件、下载速度、剩余时间和完成/失败/取消状态。
- 新增下载完成后自动打开目录，以及启动时自动粘贴剪贴板 URL 的选项。

### Changed

- 重构首页下载区域，拆分为下载表单、下载选项、下载进度等独立组件，便于维护和扩展。
- 整理前后端下载参数结构，统一格式预设、文件名模板、重试次数和并发片段的传递方式。
- 更新应用版本到 `v0.4.0`。
- 更新项目截图与文档说明。

### Fixed

- 改进 `yt-dlp`、FFmpeg、代理和网络异常时的错误提示，提供更明确的排查建议。
- 优化首页与设置页在窄屏下的布局和滚动体验。
- 调整图标资源，改善部分 Linux/AUR 安装场景下的应用图标显示。

## [0.3.0] - 2025-05-26

### Added

- 新增首页、设置、关于三页结构和侧边栏导航。
- 新增关于页版本展示，以及新的界面截图资源。
- 新增侧边栏展开/收起图标资源。

### Changed

- 重构前端页面结构，引入路由并拆分 `HomeView`、`SettingsView`、`AboutView` 组件。
- 优化整体界面布局和视觉样式。
- 更新应用版本到 `v0.3.0`。

### Fixed

- 调整 macOS 构建脚本中的 `yt-dlp` 下载逻辑。

## [0.2.0] - 2025-02-22

### Added

- 新增自定义保存目录选择。
- 新增代理设置，支持通过代理下载。
- 新增前端下载进度和错误事件展示。
- 新增中英文 README 与项目截图。

### Changed

- 将下载内核从 `lux` 切换为 `yt-dlp`。
- 调整 Tauri sidecar 二进制和构建脚本，改为打包 `my-yt-dlp`。
- 优化主界面布局与交互。
- 更新应用版本到 `v0.2.0`。

### Fixed

- 精简仓库内冗余的 `lux` 可执行文件。
- 调整 macOS 构建目标，仅保留 ARM 构建产物。

## [0.1.0] - 2025-02-12

### Added

- 首个可用版本，提供基础的视频下载能力。
- 新增基于 Tauri sidecar 的 `lux` 下载调用。
- 支持默认保存到系统 `Downloads` 目录。
- 新增基础的多平台构建配置和项目文档。

### Changed

- 初始化桌面端前后端结构、应用配置和打包资源。
- 更新应用版本到 `v0.1.0`。
